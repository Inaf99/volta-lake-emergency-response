# ============================================================
# ROOT ENV EXAMPLE — this project actually reads its env vars
# from backend/.env (see backend/.env.example). This root file
# is kept only as a single-glance summary for the class defense.
# Copy backend/.env.example to backend/.env and fill it in.
# ============================================================

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/volta_lake_emergency
JWT_SECRET=change_this_to_a_long_random_string
PORT=5000

SMS_PROVIDER=demo
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

MAP_PROVIDER=leaflet-osm
FRONTEND_ORIGIN=http://localhost:5173
